# Gravatar MCP Server Environment Variables

# Server Configuration
PORT=8787 # Default: 8787
HOST=127.0.0.1 # Use 0.0.0.0 for production deployment
NODE_ENV=development

# Production Security Configuration (only used when NODE_ENV=production)
# Comma-separated list of allowed hosts for DNS rebinding protection
ALLOWED_HOSTS=your-domain.com,api.your-domain.com
# Comma-separated list of allowed origins for CORS
ALLOWED_ORIGINS=https://your-frontend.com,https://app.your-domain.com

# Gravatar API Configuration (Optional)
# Get your API key from: https://gravatar.com/developers/
GRAVATAR_API_KEY=your-gravatar-api-key-here

# Debug Configuration
# Set to 'true' to enable detailed MCP transport logging
DEBUG=false

# Security Configuration
# Set to 'true' to disable DNS rebinding protection (NOT recommended)
# DISABLE_DNS_REBINDING_PROTECTION=false

# MCP Server Configuration
MCP_SERVER_NAME=my-gravtar-mcp-server
ENVIRONMENT=development