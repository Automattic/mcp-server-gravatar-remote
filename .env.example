# Gravatar MCP Server Environment Variables

# Server Configuration
MCP_SERVER_NAME=my-gravtar-mcp-server
NODE_ENV=development # Options: development, staging, production
PORT=8787 # Default: 8787
HOST=127.0.0.1 # Use 0.0.0.0 for production deployment

# DNS Rebinding Protection Configuration (only used when ENABLE_DNS_REBINDING_PROTECTION=true)
# Comma-separated list of allowed Host header values
# Development default: localhost:8787,127.0.0.1:8787
# ALLOWED_HOSTS=your-domain.com:443,api.your-domain.com:443

# Comma-separated list of allowed Origin header values  
# Development default: http://localhost:8787,http://127.0.0.1:8787
# ALLOWED_ORIGINS=https://your-frontend.com,https://app.your-domain.com

# Gravatar API Configuration (Optional)
# Get your API key from: https://gravatar.com/developers/
GRAVATAR_API_KEY=your-gravatar-api-key-here

# Debug Configuration
# Set to 'true' to enable detailed MCP transport logging
DEBUG=false

# Security Configuration
# Set to 'true' to enable DNS rebinding protection.
# Important: If you are running this server locally, enable DNS rebinding protection:
# https://github.com/modelcontextprotocol/typescript-sdk?tab=readme-ov-file#dns-rebinding-protection
# Default: false (disabled by default, matches MCP SDK)
# ENABLE_DNS_REBINDING_PROTECTION=true
